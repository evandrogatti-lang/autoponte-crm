"use client";

import { FormEvent, PointerEvent, useMemo, useRef, useState } from "react";
import Link from "next/link";
import type { PublicMarketplaceVehicle } from "../../lib/marketplace/public-vehicles";
import home from "./home-vision.module.css";
import work from "../match-workspace/workspace.module.css";

type Match = { vehicle: PublicMarketplaceVehicle; score: number; reasons: string[] };
type MatchResponse = { recommendations?: Match[]; error?: string };
type Stage = "home" | "matches";
const money=new Intl.NumberFormat("pt-BR",{style:"currency",currency:"BRL",maximumFractionDigits:0});const number=new Intl.NumberFormat("pt-BR");const clamp=(v:number)=>Math.max(28,Math.min(65,v));
function vehicleImage(bodyType:string){const type=bodyType.toLowerCase();if(type.includes("sedan"))return "/vehicle-sedan.png";if(type.includes("hatch"))return "/vehicle-hatch.png";return "/vehicle-suv.png"}
export default function AutoPonteExperience({initialVehicles}:{initialVehicles:PublicMarketplaceVehicle[]}){const[stage,setStage]=useState<Stage>("home");const[query,setQuery]=useState("");const[matches,setMatches]=useState<Match[]>([]);const[selected,setSelected]=useState<number|null>(null);const[loading,setLoading]=useState(false);const[error,setError]=useState("");const[split,setSplit]=useState(42);const[dragging,setDragging]=useState(false);const workspaceRef=useRef<HTMLElement|null>(null);const vehicleRef=useRef<HTMLElement|null>(null);const active=selected===null?null:matches[selected];const layout=useMemo(()=>({gridTemplateColumns:active?`${split}% 8px minmax(0,1fr)`:"1fr"}),[split,active]);const stores=Array.from(new Map(initialVehicles.map(v=>[v.partner.id,v.partner])).values()).slice(0,4);
async function runSearch(search:string,openVehicleId?:string){const clean=search.trim();if(!clean)return;setLoading(true);setError("");setSelected(null);setStage("matches");try{const response=await fetch("/api/matches",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({preferredModels:clean,budgetMax:0,maxMatches:60})});const payload=await response.json() as MatchResponse;if(!response.ok)throw new Error(payload.error||"Não foi possível calcular os Matches.");const next=payload.recommendations||[];setMatches(next);if(openVehicleId){const index=next.findIndex(item=>item.vehicle.id===openVehicleId);if(index>=0)setSelected(index)}}catch(cause){setMatches([]);setError(cause instanceof Error?cause.message:"Não foi possível calcular os Matches.")}finally{setLoading(false)}}
function submitHomeSearch(e:FormEvent<HTMLFormElement>){e.preventDefault();const d=new FormData(e.currentTarget);const value=String(d.get("q")||"");setQuery(value);void runSearch(value)}function submitResultSearch(e:FormEvent<HTMLFormElement>){e.preventDefault();void runSearch(query)}function openInventoryVehicle(v:PublicMarketplaceVehicle){const value=`${v.brand} ${v.model}`;setQuery(value);void runSearch(value,v.id)}function chooseMatch(index:number){setSelected(index);requestAnimationFrame(()=>{const panel=vehicleRef.current;if(!panel)return;panel.scrollTop=0;if(window.matchMedia("(max-width: 760px)").matches)setTimeout(()=>panel.scrollIntoView({behavior:"smooth",block:"start"}),160)})}function moveDivider(clientX:number){const box=workspaceRef.current?.getBoundingClientRect();if(box)setSplit(clamp(((clientX-box.left)/box.width)*100))}function startDrag(e:PointerEvent<HTMLDivElement>){if(window.matchMedia("(max-width: 760px)").matches)return;e.currentTarget.setPointerCapture(e.pointerId);setDragging(true);moveDivider(e.clientX)}function drag(e:PointerEvent<HTMLDivElement>){if(dragging)moveDivider(e.clientX)}function stopDrag(e:PointerEvent<HTMLDivElement>){if(e.currentTarget.hasPointerCapture(e.pointerId))e.currentTarget.releasePointerCapture(e.pointerId);setDragging(false)}function keySplit(key:string){if(key==="ArrowLeft")setSplit(v=>clamp(v-2));if(key==="ArrowRight")setSplit(v=>clamp(v+2));if(key==="Home")setSplit(28);if(key==="End")setSplit(65)}function goHome(){setStage("home");setSelected(null);setError("");window.scrollTo({top:0,behavior:"smooth"})}
return <main className={home.page}><header className={home.header}><button className={home.brandButton} type="button" onClick={goHome}><span>AP</span><b>AutoPonte</b></button><nav className={home.nav}><button onClick={goHome}>Comprar</button><button onClick={goHome}>Vender</button><button onClick={goHome}>Trocar</button><button onClick={goHome}>Como funciona</button><button onClick={goHome}>Lojas</button></nav><div className={home.headerActions}><button className={home.heart}>♡</button><Link className={home.login} href="/crm">Entrar</Link></div></header>
<div className={`${home.homeStage} ${stage==="home"?home.stageVisible:home.stageRetracted}`} aria-hidden={stage!=="home"}><section className={home.hero}><div className={home.heroBackdrop}/><div className={home.heroContent}><p className={home.eyebrow}>AUTOPONTE MATCH</p><h1>Encontre o carro certo. A AutoPonte faz o Match para você.</h1><p className={home.heroText}>Conte o que procura. Nossa inteligência cruza sua intenção com o estoque real e mostra as opções que mais fazem sentido.</p><div className={home.entryGrid}><form className={home.searchPanel} onSubmit={submitHomeSearch}><label>Já sabe o que procura?</label><p>Busque por modelo, categoria ou descreva o carro ideal.</p><div className={home.searchRow}><input name="q" value={query} onChange={e=>setQuery(e.target.value)} placeholder="Ex.: Jeep Compass automático até R$ 150 mil" required/><button>Buscar</button></div></form><div className={home.aiPanel}><div><span className={home.aiMark}>✦</span><div><strong>Ainda não sabe por onde começar?</strong><p>A IA ajuda a descobrir o carro certo para você.</p></div></div><Link href="/atendimento">Começar Busca Inteligente</Link></div></div></div></section><section className={home.how}><div className={home.sectionHeading}><p className={home.eyebrow}>COMO FUNCIONA</p><h2>Em 3 passos.</h2></div><div className={home.steps}><article><span>1</span><div><h3>Conte o que você precisa</h3><p>Orçamento, rotina e prioridades.</p></div></article><article><span>2</span><div><h3>A IA faz o Match</h3><p>Cruzamos seu contexto com o estoque.</p></div></article><article><span>3</span><div><h3>Receba os melhores Matches</h3><p>Menos opções. Mais relevância.</p></div></article></div></section><section className={home.inventory}><div className={home.sectionHeadingRow}><div><p className={home.eyebrow}>RECÉM-CHEGADOS</p><h2>Novidades no estoque.</h2></div></div>{initialVehicles.length?<div className={home.vehicleGrid}>{initialVehicles.slice(0,4).map((v,i)=><article className={home.vehicleCard} key={v.id} role="button" tabIndex={0} onClick={()=>openInventoryVehicle(v)} onKeyDown={e=>{if(e.key==="Enter"||e.key===" "){e.preventDefault();openInventoryVehicle(v)}}}><div className={home.vehicleImageWrap}><img src={vehicleImage(v.bodyType)} alt={`${v.brand} ${v.model}`}/>{i<2&&<span className={home.newChip}>NOVO</span>}</div><div className={home.vehicleBody}><p className={home.verified}>✓ Loja verificada</p><h3>{v.brand} {v.model}</h3><p>{v.modelYear} · {number.format(v.mileage)} km · {v.transmission}</p><strong>{money.format(v.price)}</strong><div className={home.vehicleFooter}><span>{v.city}{v.state?`, ${v.state}`:""}</span><span>♡</span></div></div></article>)}</div>:<div className={home.emptyState}><strong>O marketplace está pronto para receber o estoque publicado.</strong></div>}</section><section className={home.storeSection}><div className={home.sectionHeadingRow}><div><p className={home.eyebrow}>LOJAS VERIFICADAS</p><h2>Compre com mais confiança.</h2></div></div>{stores.length?<div className={home.storeGrid}>{stores.map(s=><article className={home.storeCard} key={s.id}><div className={home.storeLogo}>AP</div><div><p>✓ VERIFICADA</p><h3>{s.name}</h3><span>{s.city}{s.state?`, ${s.state}`:""}</span></div></article>)}</div>:null}</section></div>
<div className={`${home.resultsStage} ${stage==="matches"?home.stageVisible:home.stageHidden}`} aria-hidden={stage!=="matches"}><section className={work.searchBand}><div><p>RESULTADO DE BUSCA</p><h1>{loading?"Calculando seus Matches…":`${matches.length} Matches encontrados.`}</h1><span>Escolha um veículo. A lista continua aberta enquanto você compara.</span></div><form onSubmit={submitResultSearch}><input value={query} onChange={e=>setQuery(e.target.value)} required/><button disabled={loading}>{loading?"Analisando…":"Refinar"}</button></form>{error&&<strong className={work.error}>{error}</strong>}</section>{loading&&!matches.length?<section className={work.empty}><div>◎</div><h2>Buscando no estoque real…</h2></section>:!matches.length?<section className={work.empty}><h2>Nenhum Match forte nessa busca.</h2><button onClick={goHome}>Voltar</button></section>:<section ref={workspaceRef} className={`${work.workspace} ${dragging?work.isDragging:""}`} style={layout}><aside className={`${work.matches} ${active?work.matchesCompact:work.matchesFull}`}><div className={work.listHeader}><div><span>MELHORES MATCHES</span><h2>{active?"Compare sem perder a lista":"Escolha por onde começar"}</h2></div><button onClick={goHome}>Início</button></div><div className={work.list}>{matches.map((m,i)=><button key={m.vehicle.id} className={`${work.matchCard} ${i===selected?work.active:""}`} onClick={()=>chooseMatch(i)}><div className={work.thumb}><img src={vehicleImage(m.vehicle.bodyType)} alt=""/></div><div className={work.cardBody}><div><span className={work.score}>{m.score}% match</span>{i===0&&<small>TOP MATCH</small>}</div><strong>{m.vehicle.brand} {m.vehicle.model}</strong><p>{m.vehicle.modelYear} · {number.format(m.vehicle.mileage)} km · {m.vehicle.city}</p><b>{money.format(m.vehicle.price)}</b><span className={work.reason}>{m.reasons[0]||"Boa compatibilidade"}</span></div></button>)}</div></aside>{active&&<><div className={work.divider} role="separator" tabIndex={0} aria-valuemin={28} aria-valuemax={65} aria-valuenow={Math.round(split)} onKeyDown={e=>keySplit(e.key)} onPointerDown={startDrag} onPointerMove={drag} onPointerUp={stopDrag} onPointerCancel={stopDrag}><span>⋮</span></div><article ref={vehicleRef} key={active.vehicle.id} className={`${work.vehicle} ${work.vehicleEnter}`}><div className={work.vehicleTop}><div><span className={work.scoreLarge}>{active.score}% Match</span><p>{active.reasons.length} razões</p></div><div><button onClick={()=>setSplit(35)}>Lista</button><button onClick={()=>setSplit(50)}>Meio a meio</button><button onClick={()=>setSplit(28)}>Ver veículo</button><button onClick={()=>setSelected(null)}>Fechar</button></div></div><div className={work.hero}><img src={vehicleImage(active.vehicle.bodyType)} alt={`${active.vehicle.brand} ${active.vehicle.model}`}/></div><div className={work.vehicleInfo}><div className={work.title}><div><p>{active.vehicle.partner.name} · VERIFICADA</p><h2>{active.vehicle.brand} {active.vehicle.model}</h2><span>{active.vehicle.modelYear} · {number.format(active.vehicle.mileage)} km · {active.vehicle.city}</span></div><strong>{money.format(active.vehicle.price)}</strong></div><div className={work.actions}><button>Tenho interesse</button><button>Simular financiamento</button><button>Tenho carro na troca</button></div><section className={work.why}><p>POR QUE DEU MATCH?</p><h3>O que aproxima este carro da sua busca</h3><ul>{active.reasons.slice(0,5).map(r=><li key={r}>✓ {r}</li>)}</ul></section><div className={work.specs}>{[active.vehicle.transmission,active.vehicle.fuel,active.vehicle.bodyType,active.vehicle.color].filter(Boolean).map(v=><span key={v}>{v}</span>)}</div></div></article></>}</section>}</div></main>}
