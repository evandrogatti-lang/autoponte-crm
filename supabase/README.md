# Histórico legado

Este diretório está congelado e preservado apenas por compatibilidade histórica.

Novas migrações pertencem exclusivamente a `drizzle/`. Não copie migrações entre os dois diretórios. Consulte `docs/DATABASE_MIGRATIONS.md` antes de alterar o banco.
